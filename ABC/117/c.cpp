#include <bits/stdc++.h>

using namespace std;

int main(int argc, char const *argv[]) {
  int n,m;
  std::cin >> n >> m;
  std::vector<int> x(m);
  for (int i=0; i<m; i++) std::cin >> x[i];
  sort(x.begin(),x.end());
  ``

  return 0;
}
